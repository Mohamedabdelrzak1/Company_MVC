<section class="container my-5">
    <h2 class="text-white text-center mb-5">Explore Our System</h2>

    <div class="row justify-content-center g-4">
        <!-- Department Card -->
        <div class="col-md-4">
            <div class="card system-card text-white text-center h-100">
                <div class="card-body p-5">
                    <i class="bi bi-diagram-3-fill display-3 icon-glow mb-4"></i>
                    <h4 class="card-title mb-3 fw-bold">Departments</h4>
                    <p class="card-text">Manage and view all departments in your organization.</p>
                    <a href="/Department/Index" class="btn neon-btn">Go to Departments</a>
                </div>
            </div>
        </div>

        <!-- Employee Card -->
        <div class="col-md-4">
            <div class="card system-card text-white text-center h-100">
                <div class="card-body p-5">
                    <i class="bi bi-people-fill display-3 icon-glow mb-4"></i>
                    <h4 class="card-title mb-3 fw-bold">Employees</h4>
                    <p class="card-text">View, add, and manage employees with ease.</p>
                    <a href="/Employee/Index" class="btn neon-btn">Go to Employees</a>
                </div>
            </div>
        </div>

        <!-- User Card -->
        <div class="col-md-4">
            <div class="card system-card text-white text-center h-100">
                <div class="card-body p-5">
                    <i class="bi bi-person-circle display-3 icon-glow mb-4"></i>
                    <h4 class="card-title mb-3 fw-bold">Users</h4>
                    <p class="card-text">Manage platform users, roles and access control smartly.</p>
                    <a href="/User/Index" class="btn neon-btn">Go to Users</a>
                </div>
            </div>
        </div>
    </div>
</section>
<style>
    .system-card {
        background: linear-gradient(145deg, #1e1e1e, #2c2c2c);
        border: none;
        border-radius: 1rem;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        position: relative;
        overflow: hidden;
    }

        .system-card::before {
            content: "";
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(0, 234, 255, 0.1) 0%, transparent 70%);
            animation: rotateGlow 10s linear infinite;
            z-index: 0;
        }

        .system-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 40px rgba(0, 255, 255, 0.2);
        }

        .system-card .card-body {
            position: relative;
            z-index: 1;
        }

    .icon-glow {
        color: #00eaff;
        text-shadow: 0 0 15px #00eaff, 0 0 25px #00eaff;
        animation: pulseIcon 2s infinite;
    }

    .neon-btn {
        color: #00eaff;
        border: 1px solid #00eaff;
        border-radius: 30px;
        padding: 10px 30px;
        font-weight: 500;
        transition: all 0.3s ease;
        background-color: transparent;
    }

        .neon-btn:hover {
            background-color: #00eaff;
            color: #000;
            box-shadow: 0 0 10px #00eaff, 0 0 20px #00eaff;
        }

    @@keyframes rotateGlow {
        0%

    {
        transform: rotate(0deg);
    }

    100% {
        transform: rotate(360deg);
    }

    }

    @@keyframes pulseIcon {
        0%, 100%

    {
        text-shadow: 0 0 15px #00eaff, 0 0 25px #00eaff;
    }

    50% {
        text-shadow: 0 0 25px #00eaff, 0 0 35px #00eaff;
    }

    }
</style>
